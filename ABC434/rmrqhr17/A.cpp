#include <bits/stdc++.h>

    using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int n,b;
    cin >> n >> b;
    n*=1000;
    if (n%b==0) {
        cout << n/b+1;
    }
    else {
        cout << n/b+1;
    }

}